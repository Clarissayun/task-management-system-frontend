export const ROUTES = {
  root: '/',
  login: '/login',
  register: '/register',
  dashboard: '/dashboard',
  tasks: '/tasks',
  projects: '/projects',
  projectTasks: '/projects/:projectId',
  profile: '/profile',
}
